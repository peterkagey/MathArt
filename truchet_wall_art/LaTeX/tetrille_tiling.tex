\documentclass[tikz]{standalone}
\usepackage{tikz}
\pgfmathsetmacro{\n}{10}
\begin{document}
  \begin{tikzpicture}
    \foreach \b [
      evaluate={\amin using int(-\b/2)},
      evaluate={\amax using int(\n-\b/2)},
      evaluate={\c using 100*\b/\n}
    ] in {1,...,\n} {
      \foreach \a in {\amin,...,\amax} {
        \foreach \t [
        evaluate={\x using \a+\b/2},
        evaluate={\y using \b*sqrt(3)/2},
        evaluate={\r using random(0,1)},
      ] in {0,...,5} {
        % \fill (\x,\y) circle (0.05);
        % \draw (\x+1/2,{\y-1/(2*sqrt(3))})--(\x+1/2,{\y+1/(2*sqrt(3))})--(\x,{\y+1/sqrt(3)}) -- (\x-1/2,{\y+1/(2*sqrt(3))}) -- (\x-1/2,{\y-1/(2*sqrt(3))});
        % \draw[ultra thick, blue] (\x,\y) -- (\x+1/2,\y) -- (\x+1/2,{\y+1/(2*sqrt(3))}) -- (\x+1/4,{\y+3/(4*sqrt(3))}) -- cycle;
        % \draw[red, rotate around={\t*60:(\x,\y)}] (\x,\y) -- (\x+1/2,\y) -- (\x+1/2,{\y+1/(2*sqrt(3))}) -- (\x+1/4,{\y+3/(4*sqrt(3))}) -- cycle;
        \ifnum \r = 0 {
          \draw[line width = 1, rotate around={\t*60:(\x,\y)}]
            (\x+1/2,{\y+1/(4*sqrt(3))}) arc (90:180:{1/(4*sqrt(3))})
            (\x+3/8, {\y+5/(8*sqrt(3))}) arc (330:240:{1/(4*sqrt(3))})
          ;
        } \else {
          \draw[line width = 1, rotate around={\t*60:(\x,\y)}]
            (\x+1/2,{\y+1/(4*sqrt(3))}) arc (270:150:{1/(4*sqrt(3))})
            ({\x+1/2-1/(4*sqrt(3))},\y) arc (0:60:{1/2-1/(4*sqrt(3))})
            % (\x+3/8, {\y+5/(8*sqrt(3))}) arc (330:240:{1/(4*sqrt(3))})
          ;
        } \fi
        }
      }
    }
  \end{tikzpicture}
\end{document}
